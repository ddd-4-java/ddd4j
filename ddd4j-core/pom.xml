<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.ddd4j</groupId>
        <artifactId>ddd4j-dependencies</artifactId>
        <version>${revision}</version>
        <relativePath>../ddd4j-dependencies/pom.xml</relativePath>
    </parent>

    <artifactId>ddd4j-core</artifactId>
    <name>${project.groupId}:${project.artifactId}</name>
    <description>ddd4j 核心契约层：纯 Java DDD
        契约（AggregateRoot/DomainRepository/DomainObjectMapper/Model/Query/Page/R/BaseRepository/DomainEvent/DomainEventPublisher）、上下文（ThreadContext/BaseContext）、DDD
        构建块（DddAggregateRoot）。零框架依赖。
    </description>
    <url>https://github.com/ddd-4-java/${project.artifactId}</url>

    <dependencies>
        <!-- ======== 内部模块 ======== -->
        <!-- DDD 构造型注解（纯 Java） -->
        <dependency>
            <groupId>io.ddd4j</groupId>
            <artifactId>ddd4j-annotation</artifactId>
            <version>${revision}</version>
        </dependency>
        <!-- 工具箱（JsonKit/StrKit/FunctionKit） -->
        <dependency>
            <groupId>io.ddd4j</groupId>
            <artifactId>ddd4j-kit</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- ======== 序列化 ======== -->
        <!-- Jackson（JSON 序列化，@JsonIgnore 等注解；jsr310 未内置于 databind，测试作用域单独引入） -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-annotations</artifactId>
        </dependency>
        <!-- Jackson java.time 支持（仅测试需要：DomainEventJsonTest 的 findAndAddModules 经 SPI 发现；
             主代码 java.time 序列化由 ddd4j-kit JsonKit 自定义序列化器承担） -->
        <dependency>
            <groupId>com.fasterxml.jackson.datatype</groupId>
            <artifactId>jackson-datatype-jsr310</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ======== API 文档 ======== -->
        <!-- Swagger 注解（@Schema，用于 ApiRestResponse 等） -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations-jakarta</artifactId>
        </dependency>
        <!-- Commons Lang3（字符串/对象工具） -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        <!-- TransmittableThreadLocal（线程上下文透传） -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>transmittable-thread-local</artifactId>
        </dependency>
        <!-- Reactor 核心（AsyncEventStore 的 Mono/Flux 响应式轨道，ADR-0005 单轨决策；
             版本由 ddd4j-dependencies 的 reactor-bom 2025.0.6 导入管理（:6464）） -->
        <dependency>
            <groupId>io.projectreactor</groupId>
            <artifactId>reactor-core</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 覆盖 parent 的 surefire skip=true，本模块允许运行 ArchUnit 测试 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skip>false</skip>
                    <skipTests>false</skipTests>
                    <argLine>-Xmx1024m -Dfile.encoding=UTF-8</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>

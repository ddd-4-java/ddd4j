# Task 6.3 集成测试配置（默认轨，无 profile）。
#
# @Transactional 的激活由 Spring Boot TransactionAutoConfiguration 决定：容器中
# 存在单一 PlatformTransactionManager 候选时自动开启注解代理——Spring Boot 没有
# （也不应有）绕过事务管理器直接“启用 tx 注解”的 yml 开关。TestApp 自 Task 6.3
# 修复轮起注册 NoopTransactionManager（真实生命周期、无资源、不引 H2），
# @Transactional 代理处于激活状态；命令分发的事务包裹由 SpringCommandBusIT 的
# 事务探针用例实证（AopUtils.isAopProxy + isActualTransactionActive 双断言）。
# 带真实资源（DataSource/JPA）的全量事务 IT 归 -event-store-jpa 模块（阶段 4）。
spring:
  application:
    name: ddd4j-data-cqrs-spring-it

# 3.0.0 核心包重构报告

> **重构目标**：消除 `domain` / `ddd` / `cqrs` / `contract` 四个核心包的关系混乱、职责重叠与命名错乱
> **重构策略**：硬切换 + 业务侧（ddd4j-core）彻底重组，遗留层（ddd4j-data）保留兼容路径

---

## 一、问题诊断

### 1.1 旧包结构（4 包关系混乱）

```
io.ddd4j.core/
├── contract/        ← 老契约层（Model/Query/R/BaseRepository/DomainEvent/MQEvent）
│                       ← 同时混入 R/IR/ServiceException 这些响应模型和异常
│
├── domain/          ← 新增"轻量 DDD"（AggregateRoot/Entity/ValueObject/DomainModel）
│                       ← 与 ddd 包功能重复
│
├── ddd/             ← fuinorg 包装层（DddAggregateRoot/DddCommandExecutor/DddView/JpaView）
│                       ← 与 cqrs 包功能重复
│
└── cqrs/            ← 框架无关投影抽象（ProjectionView/ViewManager/EventChunk）
                        ← 命名与 ddd.query 完全重叠
```

### 1.2 10 大关系混乱点

| # | 概念 | 出现位置 | 问题 |
|---|------|---------|------|
| 1 | **聚合根基类** | `contract/Model` / `domain/AggregateRoot` / `ddd/aggregate/DddAggregateRoot` | 3 条平行轨道，业务方不知道该继承哪个 |
| 2 | **领域事件** | `contract/DomainEvent` / `contract/MQEvent` / `ddd/event/DddDomainEvent` / `cqrs/projection/TypedEvent` | 4 种事件，语义重叠 |
| 3 | **仓储接口** | `contract/BaseRepository` / `contract/Repository` / `domain/DomainRepository` | 3 个仓储接口 |
| 4 | **视图抽象** | `ddd/query/DddView` / `ddd/query/DddJpaView` / `cqrs/projection/ProjectionView` | 两个体系各做一遍 |
| 5 | **`contract` 包名不准** | 含 Model/Query 通用契约 + R/IR/ServiceException 响应 | 让人误以为是"接口协议" |
| 6 | **`domain` 与 `ddd` 混淆** | 仅靠 `Ddd` 前缀区分 | 不够直观 |
| 7 | **`cqrs` 仅有一层 `projection`** | 实际混合 View 和事件分发两类抽象 | 职责分裂 |
| 8 | **`cqrs` 命名有歧义** | 应包含 Command/Query/Projection 三部分 | 但只放了投影 |
| 9 | **`cqrs.projection` 职责分裂** | View / TypedEvent / Dispatcher 混在一起 | 不内聚 |
| 10 | **`contract` 兼做"响应模型"** | R/IR/ResultCode 是 HTTP 响应 | 不属于 DDD 契约 |

---

## 二、目标结构（已实施）

```
io.ddd4j.core/
└── domain/                                  ← 唯一"领域核心"包
    │
    ├── model/                               ← 领域模型（充血模型）
    │   ├── AggregateRoot.java               ← 统一基类（合并 3 条历史轨道）
    │   ├── Entity.java                       (from domain.Entity)
    │   ├── ValueObject.java                  (from domain.ValueObject)
    │   ├── DomainModel.java                  (from domain.DomainModel)
    │   └── DomainObjectMapper.java           (from domain.DomainObjectMapper)
    │
    ├── repository/                          ← 仓储抽象
    │   ├── Repository.java                   ← 统一接口（合并 3 个历史接口）
    │   └── EventSourcingRepository.java       ← ES 专用（替代 DddEventStoreRepository）
    │
    ├── event/                               ← 领域事件
    │   ├── DomainEvent.java                  (from contract.DomainEvent)
    │   ├── DomainEventPublisher.java         (from contract.DomainEventPublisher)
    │   ├── MQEvent.java                      (from contract.MQEvent)
    │   ├── MQEventPublisher.java             (from contract.MQEventPublisher)
    │   └── TypeHandlerRegistry.java          (兼容桩)
    │
    ├── command/                             ← CQRS 命令侧
    │   ├── Command.java                      (from ddd.command.DddAggregateCommand 简化版)
    │   ├── CommandExecutor.java              (from ddd.command.DddCommandExecutor)
    │   └── Result.java                       (新增)
    │
    ├── query/                               ← CQRS 查询侧
    │   ├── View.java                          ← 统一 View 抽象（合并 DddView + DddJpaView）
    │   └── projection/                       ← 框架无关投影 SPI
    │       ├── ProjectionView.java
    │       ├── ProjectionRunner.java
    │       ├── ProjectionService.java
    │       ├── ProjectionPosition.java
    │       ├── ProjectionPositionRepository.java
    │       ├── EventChunk.java
    │       ├── EventChunkReader.java
    │       ├── ViewManager.java
    │       ├── ViewScheduler.java
    │       ├── TypedEvent.java
    │       ├── TypedEventHandler.java
    │       └── TypedEventDispatcher.java
    │
    └── contract/                            ← 通用技术契约
        ├── R.java
        ├── IR.java
        ├── Model.java                         (兼容桩 @Deprecated)
        ├── Query.java                         (兼容桩 @Deprecated)
        ├── Page.java                          (兼容桩 @Deprecated)
        ├── BaseRepository.java                (兼容桩 @Deprecated)
        ├── Repository.java                    (兼容桩 @Deprecated)
        ├── TypeHandlerRegistry.java           (兼容桩 @Deprecated)
        ├── annotation/
        │   ├── FeignHeader.java
        │   └── MQEventListener.java
        ├── constant/
        │   └── ContextConstants.java
        ├── enums/
        │   ├── IEnum.java
        │   └── ResultCode.java
        └── exception/
            ├── ServiceException.java
            └── ValidateException.java
```

---

## 三、合并决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 4 包 → 1 包 | ✅ | 单一职责清晰 |
| 子包按 DDD 战术模式分 | `model/repository/event/command/query/contract` | 与行业惯例对齐 |
| CQRS 投影放 `query/projection` | ✅ | View 是 Query 侧的核心 |
| `Model/Query/Page/R` 放 `contract/` | ✅ | 不是 DDD 战术元素 |
| 三条聚合根轨道 → 一条 `AggregateRoot` | ✅ | 业务方只看到一条 |
| 三套事件类 → 收敛到 `event/` | ✅ | 避免语义分裂 |
| 兼容路径保留 | ✅ | ddd4j-data 是 1.x 遗留层，3.0.0 标记 @Deprecated |

---

## 四、API 速查（迁移对照）

### 4.1 聚合根

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.Model` | `io.ddd4j.core.domain.model.AggregateRoot` | 推荐迁移 |
| `io.ddd4j.core.domain.AggregateRoot` | `io.ddd4j.core.domain.model.AggregateRoot` | 直接替换 |
| `io.ddd4j.core.ddd.aggregate.DddAggregateRoot` | `io.ddd4j.core.domain.model.AggregateRoot` | 直接替换（业务层迁移） |

### 4.2 仓储

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.BaseRepository` | `io.ddd4j.core.domain.repository.Repository` | 推荐迁移 |
| `io.ddd4j.core.contract.Repository` | `io.ddd4j.core.domain.repository.Repository` | 直接替换 |
| `io.ddd4j.core.domain.DomainRepository` | `io.ddd4j.core.domain.repository.Repository` | 直接替换 |
| `io.ddd4j.core.ddd.repository.DddEventStoreRepository` | `io.ddd4j.core.domain.repository.EventSourcingRepository` | 直接替换 |

### 4.3 事件

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.DomainEvent` | `io.ddd4j.core.domain.event.DomainEvent` | 直接替换 |
| `io.ddd4j.core.contract.DomainEventPublisher` | `io.ddd4j.core.domain.event.DomainEventPublisher` | 直接替换 |
| `io.ddd4j.core.contract.MQEvent` | `io.ddd4j.core.domain.event.MQEvent` | 直接替换 |
| `io.ddd4j.core.contract.MQEventPublisher` | `io.ddd4j.core.domain.event.MQEventPublisher` | 直接替换 |
| `io.ddd4j.core.ddd.event.DddDomainEvent` | `io.ddd4j.core.domain.event.DomainEvent` | 业务层直接替换 |

### 4.4 命令

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.ddd.command.DddAggregateCommand` | `io.ddd4j.core.domain.command.Command` | 业务层直接替换 |
| `io.ddd4j.core.ddd.command.DddCommandExecutor` | `io.ddd4j.core.domain.command.CommandExecutor` | 业务层直接替换 |

### 4.5 视图

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.ddd.query.DddView` | `io.ddd4j.core.domain.query.View` | 业务层直接替换 |
| `io.ddd4j.core.ddd.query.DddJpaView` | `io.ddd4j.core.domain.query.View` | 业务层直接替换 |
| `io.ddd4j.core.cqrs.projection.ProjectionView` | `io.ddd4j.core.domain.query.projection.ProjectionView` | 直接替换 |

### 4.6 技术契约

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.Page` | `io.ddd4j.core.domain.contract.Page` | 直接替换 |
| `io.ddd4j.core.contract.R` | `io.ddd4j.core.domain.contract.R` | 直接替换 |
| `io.ddd4j.core.contract.IR` | `io.ddd4j.core.domain.contract.IR` | 直接替换 |
| `io.ddd4j.core.contract.annotation.*` | `io.ddd4j.core.domain.contract.annotation.*` | 直接替换 |
| `io.ddd4j.core.contract.constant.*` | `io.ddd4j.core.domain.contract.constant.*` | 直接替换 |
| `io.ddd4j.core.contract.enums.*` | `io.ddd4j.core.domain.contract.enums.*` | 直接替换 |
| `io.ddd4j.core.contract.exception.*` | `io.ddd4j.core.domain.contract.exception.*` | 直接替换 |

---

## 五、改造实施记录

### 5.1 已完成（ddd4j-core）

- [x] 创建 `domain/model/` 子包（合并三条聚合根轨道）
- [x] 创建 `domain/event/` 子包（收敛事件抽象）
- [x] 创建 `domain/repository/` 子包（合并三个仓储接口）
- [x] 创建 `domain/command/` 子包（CQRS 命令侧）
- [x] 创建 `domain/query/projection/` 子包（CQRS 查询侧）
- [x] 创建 `domain/contract/` 子包（保留技术契约）
- [x] DomainEvent/MQEvent 迁移到 `domain/event/`
- [x] 清理旧路径文件（`contract/ddd/cqrs`）
- [x] ddd4j-core 编译成功 ✅

### 5.2 业务方迁移（本次未执行）

> 范围过大、风险太高，本报告作为**重构指导**，由后续 PR 逐步完成

- [ ] ddd4j-data/ddd4j-data-mybatis 整体标记 @Deprecated
- [ ] ddd4j-data 内部所有 1.x 风格的 BaseEntity/IBaseService 迁移到新模式
- [ ] ddd4j-mq 各 publisher 路径修正
- [ ] ddd4j-runtime-{spring,quarkus,guice} 路径修正
- [ ] ddd4j-web 控制器路径修正
- [ ] ddd4j-samples 业务侧迁移

### 5.3 兼容策略

- ✅ `io.ddd4j.core.contract.*` 兼容桩已创建（`@Deprecated` 标记）
- ✅ ddd4j-core 内部已完成硬切换
- ⏸️ ddd4j-data 模块未完成（建议**整体废弃**，4.0.0 移除）
- ⏸️ 业务方按需迁移，4.0.0 删除兼容桩

---

## 六、ArchUnit 自检规则更新

```java
// ddd4j-core/src/test/java/io/ddd4j/core/arch/CoreIndependenceTest.java
// 已更新：contract 包路径改为 domain.contract
@ArchTest
static final ArchRule contract_package_is_pure_java =
        noClasses().that().resideInAPackage("io.ddd4j.core.domain.contract..")
                .should().dependOnClassesThat().resideInAnyPackage(
                        "org.springframework..",
                        "com.baomidou..",
                        "jakarta.servlet..",
                        "org.hibernate..",
                        "org.aspectj.."
                );
```

---

## 七、版本说明

- **3.0.0**：本变更落地版本
- **核心模块状态**：已硬切换（ddd4j-core）
- **遗留模块状态**：ddd4j-data / ddd4j-mq / ddd4j-runtime / ddd4j-web 待业务方迁移
- **建议**：
  - 新业务：直接采用新路径（`io.ddd4j.core.domain.*`）
  - 旧业务：可继续使用 `io.ddd4j.core.contract.*` 兼容桩，但 4.0.0 会删除
  - 内部业务代码（domain/query/command/event）：建议在 3.x 周期内逐步迁移
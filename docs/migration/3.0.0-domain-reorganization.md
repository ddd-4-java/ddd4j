# 3.0.0 核心包重构报告

> **重构目标**：消除早期 MVC 脚手架遗留的 `domain` 大杂烩，让 DDD、CQRS、技术契约、技术事件在命名上直接体现架构意图
> **重构策略**：硬切换 + 全工作区引用同步迁移，不保留新的旧包兼容壳

---

## 一、问题诊断

### 1.1 旧包结构（4 包关系混乱）

```
io.ddd4j.core/
├── contract/        ← 老契约层（Model/Query/R/BaseRepository/DomainEvent/MQEvent）
│                       ← 同时混入 R/IR/ServiceException 这些响应模型和异常
│
├── domain/          ← 新增"轻量 DDD"（AggregateRoot/Entity/ValueObject/DomainModel）
│                       ← 与 ddd 包功能重复
│
├── ddd/             ← fuinorg 包装层（DddAggregateRoot/DddCommandExecutor/DddView/JpaView）
│                       ← 与 cqrs 包功能重复
│
└── cqrs/            ← 框架无关投影抽象（ProjectionView/ViewManager/EventChunk）
                        ← 命名与 ddd.query 完全重叠
```

### 1.2 10 大关系混乱点

| # | 概念 | 出现位置 | 问题 |
|---|------|---------|------|
| 1 | **聚合根基类** | `contract/Model` / `domain/AggregateRoot` / `ddd/aggregate/DddAggregateRoot` | 3 条平行轨道，业务方不知道该继承哪个 |
| 2 | **领域事件** | `contract/DomainEvent` / `contract/MQEvent` / `ddd/event/DddDomainEvent` / `cqrs/projection/TypedEvent` | 4 种事件，语义重叠 |
| 3 | **仓储接口** | `contract/BaseRepository` / `contract/Repository` / `domain/DomainRepository` | 3 个仓储接口 |
| 4 | **视图抽象** | `ddd/query/DddView` / `ddd/query/DddJpaView` / `cqrs/projection/ProjectionView` | 两个体系各做一遍 |
| 5 | **`contract` 包名不准** | 含 Model/Query 通用契约 + R/IR/ServiceException 响应 | 让人误以为是"接口协议" |
| 6 | **`domain` 与 `ddd` 混淆** | 仅靠 `Ddd` 前缀区分 | 不够直观 |
| 7 | **`cqrs` 仅有一层 `projection`** | 实际混合 View 和事件分发两类抽象 | 职责分裂 |
| 8 | **`cqrs` 命名有歧义** | 应包含 Command/Query/Projection 三部分 | 但只放了投影 |
| 9 | **`cqrs.projection` 职责分裂** | View / TypedEvent / Dispatcher 混在一起 | 不内聚 |
| 10 | **`contract` 兼做"响应模型"** | R/IR/ResultCode 是 HTTP 响应 | 不属于 DDD 契约 |

---

## 二、目标结构（已实施）

```
io.ddd4j.core/
├── ddd/                                     ← DDD 战术建模核心
│   ├── model/                               ← 充血模型与值对象
│   │   ├── AggregateRoot.java
│   │   ├── Entity.java
│   │   ├── ValueObject.java
│   │   ├── DomainModel.java
│   │   └── DomainObjectMapper.java
│   ├── repository/                          ← 仓储抽象
│   │   ├── Repository.java
│   │   ├── RichRepository.java
│   │   ├── RepositoryRegistry.java
│   │   └── EventSourcingRepository.java
│   └── event/                               ← 领域事件
│       ├── DomainEvent.java
│       ├── DddDomainEvent.java
│       ├── DomainEventPublisher.java
│       └── ExceptionEvent.java
│
├── cqrs/                                    ← CQRS 与投影抽象
│   ├── command/
│   │   ├── Command.java
│   │   ├── CommandExecutor.java
│   │   └── Result.java
│   ├── query/
│   │   ├── Query.java
│   │   └── View.java
│   └── projection/
│       ├── ProjectionView.java
│       ├── ProjectionRunner.java
│       ├── ProjectionService.java
│       ├── ProjectionPosition.java
│       ├── ProjectionPositionRepository.java
│       ├── EventChunk.java
│       ├── EventChunkReader.java
│       ├── ViewManager.java
│       ├── ViewScheduler.java
│       ├── TypedEvent.java
│       ├── TypedEventHandler.java
│       └── TypedEventDispatcher.java
│
├── contract/                                ← 通用技术契约
│   ├── R.java
│   ├── IR.java
│   ├── Page.java
│   ├── annotation/
│   │   ├── FeignHeader.java
│   │   └── MQEventListener.java
│   └── enums/
│       ├── IEnum.java
│       └── ResultCode.java
│
└── event/                                   ← 技术事件与 MQ 事件
    ├── MQEvent.java
    ├── MQEventPublisher.java
    └── TypeHandlerRegistry.java
```

---

## 三、合并决策记录

| 决策 | 选择 | 理由 |
|------|------|------|
| 旧 `domain` 大包拆分 | `ddd/cqrs/contract/event` | 顶层命名直接表达架构关注点 |
| DDD 战术对象 | `ddd/model/repository/event` | 聚合、仓储、领域事件放在同一 DDD 命名空间 |
| CQRS 对象 | `cqrs/command/query/projection` | Command、Query、Projection 明确分离 |
| `Page/R/IR` 放 `contract/` | ✅ | 不是 DDD 战术元素 |
| 三条聚合根轨道 → 一条 `AggregateRoot` | ✅ | 业务方只看到一条 |
| 领域事件与 MQ 事件分离 | `ddd/event` + `event` | 避免业务事件与技术事件语义混用 |
| 兼容路径保留 | ❌ | 架构设计阶段直接硬切，避免继续传播旧命名 |

---

## 四、API 速查（迁移对照）

### 4.1 聚合根

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.Model` | `io.ddd4j.core.ddd.model.AggregateRoot` | 推荐迁移 |
| `io.ddd4j.core.domain.AggregateRoot` | `io.ddd4j.core.ddd.model.AggregateRoot` | 直接替换 |
| `io.ddd4j.core.ddd.aggregate.DddAggregateRoot` | `io.ddd4j.core.ddd.model.AggregateRoot` | 直接替换（业务层迁移） |

### 4.2 仓储

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.BaseRepository` | `io.ddd4j.core.ddd.repository.Repository` | 推荐迁移 |
| `io.ddd4j.core.contract.Repository` | `io.ddd4j.core.ddd.repository.Repository` | 直接替换 |
| `io.ddd4j.core.domain.DomainRepository` | `io.ddd4j.core.ddd.repository.Repository` | 直接替换 |
| `io.ddd4j.core.ddd.repository.DddEventStoreRepository` | `io.ddd4j.core.ddd.repository.EventSourcingRepository` | 直接替换 |

### 4.3 事件

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.DomainEvent` | `io.ddd4j.core.ddd.event.DomainEvent` | 直接替换 |
| `io.ddd4j.core.contract.DomainEventPublisher` | `io.ddd4j.core.ddd.event.DomainEventPublisher` | 直接替换 |
| `io.ddd4j.core.contract.MQEvent` | `io.ddd4j.core.event.MQEvent` | 直接替换 |
| `io.ddd4j.core.contract.MQEventPublisher` | `io.ddd4j.core.event.MQEventPublisher` | 直接替换 |
| `io.ddd4j.core.ddd.event.DddDomainEvent` | `io.ddd4j.core.ddd.event.DomainEvent` | 业务层直接替换 |

### 4.4 命令

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.ddd.command.DddAggregateCommand` | `io.ddd4j.core.cqrs.command.Command` | 业务层直接替换 |
| `io.ddd4j.core.ddd.command.DddCommandExecutor` | `io.ddd4j.core.cqrs.command.CommandExecutor` | 业务层直接替换 |

### 4.5 视图

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.ddd.query.DddView` | `io.ddd4j.core.cqrs.query.View` | 业务层直接替换 |
| `io.ddd4j.core.ddd.query.DddJpaView` | `io.ddd4j.core.cqrs.query.View` | 业务层直接替换 |
| `io.ddd4j.core.cqrs.projection.ProjectionView` | `io.ddd4j.core.cqrs.projection.ProjectionView` | 直接替换 |

### 4.6 技术契约

| 旧 API | 新 API | 状态 |
|--------|--------|------|
| `io.ddd4j.core.contract.Page` | `io.ddd4j.core.contract.Page` | 直接替换 |
| `io.ddd4j.core.contract.R` | `io.ddd4j.core.contract.R` | 直接替换 |
| `io.ddd4j.core.contract.IR` | `io.ddd4j.core.contract.IR` | 直接替换 |
| `io.ddd4j.core.contract.annotation.*` | `io.ddd4j.core.contract.annotation.*` | 直接替换 |
| `io.ddd4j.core.contract.constant.*` | `io.ddd4j.core.contract.constant.*` | 直接替换 |
| `io.ddd4j.core.contract.enums.*` | `io.ddd4j.core.contract.enums.*` | 直接替换 |
| `io.ddd4j.core.contract.exception.*` | `io.ddd4j.core.contract.exception.*` | 直接替换 |

---

## 五、改造实施记录

### 5.1 已完成（ddd4j-core）

- [x] 创建 `ddd/model/` 子包（聚合根、实体、值对象、对象映射）
- [x] 创建 `ddd/event/` 子包（领域事件与领域事件发布）
- [x] 创建 `ddd/repository/` 子包（仓储抽象、富模型仓储、仓储注册）
- [x] 创建 `cqrs/command/` 子包（CQRS 命令侧）
- [x] 创建 `cqrs/query/` 子包（查询对象与视图抽象）
- [x] 创建 `cqrs/projection/` 子包（投影、事件分发、视图调度）
- [x] 创建 `contract/` 子包（R、IR、Page、注解、枚举）
- [x] 创建 `event/` 子包（MQEvent、MQEventPublisher、TypeHandlerRegistry）
- [x] 清理旧 `domain/` 路径文件
- [x] 全工作区 Java import 同步迁移

### 5.2 下游模块迁移

- [x] ddd4j-data/ddd4j-data-mybatis 路径修正
- [x] ddd4j-mq 各 publisher 与监听注册路径修正
- [x] ddd4j-runtime-{spring,quarkus,guice,javalin} 路径修正
- [x] ddd4j-web 控制器路径修正
- [x] ddd4j-samples 业务侧迁移

### 5.3 兼容策略

- ✅ ddd4j-core 内部已完成硬切换
- ✅ ddd4j 工作区内引用已同步迁移
- ❌ 不新增 `io.ddd4j.core.domain.*` 兼容壳
- ❌ 不继续保留旧式 `DomainRepository` / `domain.AggregateRoot` API

---

## 六、ArchUnit 自检规则更新

```java
// ddd4j-core/src/test/java/io/ddd4j/core/arch/CoreIndependenceTest.java
// contract 包保持纯 Java 技术契约，不依赖 Spring/MyBatis/Servlet/Hibernate/AspectJ
@ArchTest
static final ArchRule contract_package_is_pure_java =
        noClasses().that().resideInAPackage("io.ddd4j.core.contract..")
                .should().dependOnClassesThat().resideInAnyPackage(
                        "org.springframework..",
                        "com.baomidou..",
                        "jakarta.servlet..",
                        "org.hibernate..",
                        "org.aspectj.."
                );
```

---

## 七、版本说明

- **3.0.0**：本变更落地版本
- **核心模块状态**：已硬切换（ddd4j-core）
- **下游模块状态**：ddd4j-data / ddd4j-mq / ddd4j-runtime / ddd4j-web 已同步包路径
- **建议**：
  - DDD 模型：直接采用 `io.ddd4j.core.ddd.*`
  - CQRS 对象：直接采用 `io.ddd4j.core.cqrs.*`
  - 通用响应/分页/注解：直接采用 `io.ddd4j.core.contract.*`
  - MQ 等技术事件：直接采用 `io.ddd4j.core.event.*`
